import { Box } from '@mui/material'

function Logo() {
    return (
        <Box sx={{
            height: "100%",
            display: "flex",
            justifyContent: "flex-start",
            alignItems: "center"
        }}
        >
            Social Media Links (where are they...)
        </Box>
    )
}

export default Logo